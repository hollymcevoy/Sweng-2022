declare type Work<T> = (...args: any[]) => T;
declare type Enhancer<T> = (next: Work<T>) => Work<T>;
declare type Middleware<Setup, Result> = (setup: Setup) => Enhancer<Result>;
export default function concatMiddleware<Setup, Result>(...middleware: Middleware<Setup, Result>[]): Middleware<Setup, Result>;
export {};
//# sourceMappingURL=concatMiddleware.d.ts.map