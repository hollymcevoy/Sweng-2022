import { StrictStyleOptions } from 'botframework-webchat-api';
export default function createTextContentStyle({ bubbleMaxWidth, bubbleMinHeight, markdownExternalLinkIconImage, primaryFont, paddingRegular }: StrictStyleOptions): {
    fontFamily: string;
    margin: number;
    minHeight: number;
    padding: number;
    '& > :first-child': {
        marginTop: number;
    };
    '& > :last-child': {
        marginBottom: number;
    };
    '&.markdown': {
        '& img:not(.webchat__markdown__external-link-icon)': {
            maxWidth: number;
            width: string;
        };
        '& img.webchat__markdown__external-link-icon': {
            backgroundImage: string;
            height: string;
            marginLeft: string;
        };
        '& pre': {
            overflow: string;
        };
    };
};
//# sourceMappingURL=TextContent.d.ts.map